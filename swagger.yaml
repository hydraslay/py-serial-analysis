swagger: '2.0'
info:
  title: ts api
  description: ts api
  version: 1.0.0
produces:
  - application/json
paths:
  /raw:
    get:
      tags:
        - RawData
      operationId: getRawData
      summary: get RawData
      description: get RawData
      responses:
        '200':
          description: get RawData
          schema:
            type: array
            items:
              $ref: '#/definitions/RawData'

definitions:
  RawData:
    type: object
    properties:
      id:
        type: integer
      name:
        type: string
      email:
        type: string
      last_login:
        type: string
      deleted_at:
        type: string
